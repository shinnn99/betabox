# 16/09/2026 — Giới hạn thời gian đơn, tên file video, nhận diện camera bằng MAC

**Commit trong ngày:** `7490a2a` xử lý dung lượng video · `997daab` feat(proof): tên file video theo mã vận đơn, ngày, giờ · `4b86040` feat(camera): định danh camera bằng MAC và tự dò lại IP khi DHCP đổi

---

## 1. Trần 3 phút mỗi đơn và thông báo đọc thành tiếng

- Mỗi đơn tối đa **3 phút**. Quá hạn thì hệ thống tự chốt đơn (`capped_timeout`) và thông báo *"Video quá thời gian quy định, tự động dừng"*.
- Luật chạy ở cả màn hình bàn lẫn heartbeat agent — bàn tắt màn hình vẫn bị chặn.
- Màn hình bàn đọc thành tiếng (giọng `vi-VN`): mở ca, quét mã, mã trùng, chưa mở ca, QR nhân viên sai, sắp hết giờ (còn 30 giây)…
- Cảnh báo "camera chưa ghi hình" khi có đơn mở mà không phiên ghi nào chạy.
- **Kiểm thật:** đơn `DEMO2CAM-B` bị chốt đúng 180 giây; clip 180,00 giây, 70,3 MB.

## 2. Tên file video khi tải về

`<mã vận đơn>-<yyyyMMdd>-<HHmmss>.mp4` theo giờ Việt Nam, mốc lấy từ lúc quét để tạo lại clip vẫn ra cùng tên. Modal xem video có nút **Tải video**.

## 3. Nhận diện camera bằng MAC (kế hoạch E.1, Bước A)

- **Kế hoạch E.1:** định danh camera bằng MAC + ONVIF UUID; xem camera từ xa qua VPS relay (ước 6–12 USD/tháng). Chủ dự án duyệt phần MAC; **phần xem từ xa tạm hoãn**.
- **Rollback có chủ đích:** toàn bộ E.1 cất sang nhánh `e1-backup-20260916`, cây làm việc trả về `997daab` để test quét mã, rồi chỉ lấy lại phần MAC.
- **Tìm camera theo MAC:** hỏi bảng ARP trước (47–60 ms), quét cổng chỉ là dự phòng; xác nhận cổng RTSP mở ở IP mới trước khi báo cloud.
- **Sự cố thật lúc 14:10:** code đọc cột `cameras.mac_address` trên database chưa có cột → agent mất danh sách camera cần ghi. Sửa: thiếu cột thì chạy lại truy vấn với cột cũ, chỉ cảnh báo một lần. *Mất MAC chỉ mất khả năng tự dò IP; mất truy vấn là mất ghi bằng chứng.*
- **Chạy đầu-cuối trên agent thật** lộ hai lỗi mà mọi test đều xanh:
  1. Route `/api/agent/camera-ip-healed` bị proxy chặn 401 trước khi tới bước kiểm HMAC → thêm vào danh sách bỏ qua proxy **và** thêm test đối chiếu toàn bộ đường dẫn agent.
  2. Chữa được IP nhưng ghi hình không chạy lại → thêm `notifyEndpointHealed()` bật ghi ngay.
  - Kết quả: từ lúc agent khởi động tới segment mới của `hik_3` là **55 giây**.
- **Nghiệm thu hai camera cùng model trên cùng mạng:** phân biệt đúng 4/4, kể cả MAC lệch 1 ký tự.

## 4. Camera quét mã không tạo được đơn

- **Triệu chứng:** mọi mã quét bằng camera đều ra `unmapped_scanner`, hơn một ngày, không cảnh báo nào trên giao diện.
- **Nguyên nhân:** `dahua_3` được chuyển sang BAN_03 bằng cách tạo bản ghi thiết bị mới, không qua đường gán chuẩn → máy quét ảo cũ bị lưu trữ ở tổ chức cũ, tổ chức mới không có.
- **Sửa:** tự kiểm và sửa máy quét ảo mỗi lần nạp danh sách camera.

## 5. Gắn MAC cho camera đã có

Hàng thiết bị đã có trước đây chỉ là nhãn chết "Đã thêm". Nay là nút **Gắn MAC / Kết nối lại**, dùng lại màn hình nhập tài khoản và **cập nhật** bản ghi cũ (không tạo trùng).
