# 15/09/2026 — Livestream hai camera và video bằng chứng ghép PiP

**Commit trong ngày:** `e5c6879` feat(two-camera): finalize live proof pipeline

---

## 1. Kiểm thử toàn hệ thống (QA-2CAMERA-FULL)

Chạy release-gate cho toàn bộ luồng mới: typecheck, ESLint, test, build, migration trên PostgreSQL cô lập, secret scan, MediaMTX → FFmpeg → giải mã QR trên cả Hikvision và Dahua thật.
**Kết quả:** chưa đạt — còn 1 test hỏng, 16 lỗi ESLint, chuỗi migration dựng từ đầu thất bại, phát hiện lỗi định tuyến sai agent/camera. Báo cáo: `plans/reports/QA-2CAMERA-FULL-2026-09-15.md`. Các lỗi này được xử lý trong các mục dưới.

## 2. Chuẩn hoá camera và dựng bàn thử

- Dahua đổi các profile H.265 sang **H.264** (kiến trúc bắt buộc H.264), giữ nguyên độ phân giải/FPS/bitrate.
- Tạo **BAN_03**, gán `hik_3` (toàn cảnh) và `dahua_3` (góc QR) + máy quét ảo `qrcam_dahua_3`.
- Kết quả: Hik 2688×1520, Dahua 1920×1080 qua MediaMTX; Dahua đọc được QR ngay khung đầu.

## 3. Livestream hai camera trên Giám sát đóng hàng

- API live kiểm tổ chức / vai trò / bàn; tài khoản đóng gói chỉ xem được bàn của mình.
- Bố cục: camera toàn cảnh đầy khung, camera QR ở ô 1/9 góc trên phải.
- Bấm thẻ bàn để mở live, bấm lại để đóng — **đóng live không dừng ghi hình**.
- Nút toàn màn hình bật/tắt được, kể cả thoát bằng phím Esc.
- Bật tuỳ chọn `tryDenoise` của ZXing: ảnh Dahua thật mặc định không đọc được, bật lên thì đọc được ở mọi độ phân giải.

## 4. Video bằng chứng phải có cả hai camera

- **Sự cố:** clip cũ chỉ có Hikvision.
- **Sửa (FIX-2CAM-PROOF-PIP):** cloud lấy segment riêng của từng góc theo đúng `agent_id`; agent nối từng góc rồi ghép H.264 1920×1080 — Hik đầy khung, Dahua 640×360 góc trên phải, viền trắng, dải thông tin đáy. Trần clip 180 giây.
- **Sửa (FIX-2CAM-SEGMENT-COMPOSE):** chọn camera QR cho clip theo ảnh chụp lúc quét; không có thì lấy camera `proof_qr` đang gắn bàn — **bất kể bàn quét bằng súng hay camera**.
- Kết quả thật: clip 132,72 giây, 51,4 MB, thấy rõ cả hai camera.

## 5. Bảo mật credential camera

Che toàn bộ phần tài khoản trong mọi URL RTSP ở log FFmpeg; làm sạch log agent cũ, xoá script nháp chứa URL. Log cũ và mới: 0 credential lộ.

## 6. Tài liệu bàn giao

README ngắn cho mọi thư mục của luồng 2 camera và chú thích tại các chỗ dễ hỏng (layout live phải khớp bố cục clip, tắt live không dừng ghi, relay không ghi credential).
