# 25/09/2026

## Hai kế hoạch mới

- **Đẩy segment lên Google Drive** — `plans/active/DRIVE-day-segment-len-google-drive.md`. Đo trước khi viết: một camera ghi ra 1 GB mỗi giờ, đường lên kho chỉ chở được 0,68 GB mỗi giờ. Đẩy nguyên segment gốc là không chạy được, nên kế hoạch chia ba tầng: bản nén nhẹ cho giờ có ca, bản gốc cho đoạn quanh lượt quét có vấn đề, còn lại không đẩy.
- **Kết nối camera USB** — `plans/active/CAMERA-USB-ket-noi-webcam.md`. Cách làm: đẩy webcam vào chính relay đang chạy, để xem trực tiếp / đọc QR / ghi hình dùng lại nguyên vẹn. Chốt chặn phải phá trước: hàm dựng cấu hình relay đang ném lỗi khi nguồn không phải RTSP.

Cả hai đều chưa viết dòng mã nào, và đều có mục câu hỏi cần chủ dự án trả lời trước khi bắt đầu.

## Sửa lỗi quét nhầm và mất video cũ (kho Đại Kim)

- **Camera đọc trúng mã QR link của TikTok** (nhãn TikTok in hai mã QR) và hệ thống nhận bừa làm mã vận đơn. Một lượt đọc nhầm hồi 27/08 đẻ ra 8 "kiện hoàn" ma ngày 25/09, lẫn vào nhật ký đóng hàng. Nay chuỗi không có dáng mã vận đơn bị chặn ngay, ghi là mã không hợp lệ và không tạo đơn.
- **Clip của đơn từ 21/09 trở về trước không tải được.** Bản ghi đoạn video cũ thiếu mã máy kho nên hệ thống lọc ra 0 đoạn, rồi báo sai thành "quá hạn lưu trữ 30 ngày" cho đơn mới 8 ngày. Đã sửa cách tìm đoạn, và bỏ câu đoán mò đó — không tìm thấy thì nói đúng là không tìm thấy.
- **Đoạn video "dài 8 ngày"**: vài bản ghi bị đóng bằng giờ hiện tại thay vì giờ thật, phủ lên cửa sổ cắt clip của mọi đơn trong khoảng đó và làm clip ghép ra hỏng. Đã cắt về đúng 60 giây.


---

## Phát hành cho người dùng

<!-- ban: agent=0.12.1 -->
### Bỏ qua mã QR đường link trên nhãn TikTok

Phiếu TikTok in hai mã QR cạnh nhau: một mã vận đơn và một mã link tới trang bán hàng. Camera bắt trúng cái nào trước thì gửi cái đó lên, nên có lúc hệ thống nhận cái link thay vì mã vận đơn.

#### [Sửa lỗi] Chỉ nhận mã vận đơn, không nhận link

Máy kho nhận ra mã đường link và bỏ qua, lấy mã vận đơn in ngay cạnh. Trước đây hai mã ngang cỡ nhau trong cùng khung hình còn bị coi là "hai nhãn cùng lúc" và không quét được gì — nay hết.

<!-- ban: agent=web -->
### Nhật ký đóng hàng không còn lẫn kiện hoàn

Trang Giám sát đóng hàng liệt kê mọi lượt quét trong ngày, nên kiện hoàn hiện chung bảng với đơn đi dù hàng hoàn đã có trang theo dõi riêng.

#### [Sửa lỗi] Mỗi luồng một bảng

Nhật ký đóng hàng chỉ còn đơn đi; kiện hoàn xem ở Giám sát hoàn hàng. Con số tổng cũng trừ phần hoàn ra để không đá nhau với danh sách bên dưới.

#### [Sửa lỗi] Chuỗi không phải mã vận đơn thì không tạo đơn

Chuỗi quá ngắn, quá dài hoặc có ký tự lạ nay bị ghi là mã không hợp lệ, không tạo đơn và không kích hoạt lưới an toàn hàng hoàn. Các đơn rác đã lỡ sinh ra ở kho đã được dọn sạch.
