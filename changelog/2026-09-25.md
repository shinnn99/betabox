# 25/09/2026

## Hai kế hoạch mới

- **Đẩy segment lên Google Drive** — `plans/active/DRIVE-day-segment-len-google-drive.md`. Đo trước khi viết: một camera ghi ra 1 GB mỗi giờ, đường lên kho chỉ chở được 0,68 GB mỗi giờ. Đẩy nguyên segment gốc là không chạy được, nên kế hoạch chia ba tầng: bản nén nhẹ cho giờ có ca, bản gốc cho đoạn quanh lượt quét có vấn đề, còn lại không đẩy.
- **Kết nối camera USB** — `plans/active/CAMERA-USB-ket-noi-webcam.md`. Cách làm: đẩy webcam vào chính relay đang chạy, để xem trực tiếp / đọc QR / ghi hình dùng lại nguyên vẹn. Chốt chặn phải phá trước: hàm dựng cấu hình relay đang ném lỗi khi nguồn không phải RTSP.

Cả hai đều chưa viết dòng mã nào, và đều có mục câu hỏi cần chủ dự án trả lời trước khi bắt đầu.
