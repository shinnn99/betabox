# Task C.1 — Đọc QR, ghi theo ca và live

## Mục tiêu

Triển khai tuần tự năm subtask trong `plans/active`: C.1.1 đến C.1.5, tương ứng Bước 8–12 của tài liệu kiến trúc. Chỉ chuyển umbrella plan sang `completed` khi cả năm subtask đều xanh.

## Kiểm tra

Chạy test/typecheck; hoàn tất mới chuyển file sang `plans/completed/`.

## Kết quả audit 2026-09-14

Chưa hoàn thành. Thiếu tích hợp `index.ts`, supervisor/restart relay, đóng gói MediaMTX, shift recording, QR frame source/decoder/service/tests, station UI, cấu hình navigation/installer và toàn bộ remote live qua VPS. Giữ ở `active` cho tới khi toàn bộ Bước 8–12 đạt kiểm tra.
